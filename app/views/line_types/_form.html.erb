<%= form_for(@line_type) do |f| %>
  <% if @line_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@line_type.errors.count, "error") %> prohibited this line_type from being saved:</h2>
      <ul>
      <% @line_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field"><%= f.label :line_type %><%= f.text_field :line_type %></div>
  <div class="field"><%= f.label :description %><%= f.text_field :description %></div>
  <div class="actions"><%= f.submit %></div>
<% end %>